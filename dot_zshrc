export EDITOR="nvim"
export VISUAL="nvim"
export WORKDIR="$HOME/Work"

# Enable Powerlevel10k instant prompt. Should stay close to the top of ~/.zshrc.
# Initialization code that may require console input (password prompts, [y/n]
# confirmations, etc.) must go above this block; everything else may go below.
# if [[ -r "${XDG_CACHE_HOME:-$HOME/.cache}/p10k-instant-prompt-${(%):-%n}.zsh" ]]; then
#   source "${XDG_CACHE_HOME:-$HOME/.cache}/p10k-instant-prompt-${(%):-%n}.zsh"
# fi

alias vim="nvim"
# Requires vivid. brew install vivid
cache="${XDG_CACHE_HOME:-$HOME/.cache}/ls_colors.tokyonight-night"
if [[ ! -f $cache || $cache -ot ~/.zshrc ]]; then
  mkdir -p "${cache:h}"
  vivid generate tokyonight-night >| "$cache"
fi
export LS_COLORS="$(<"$cache")"
# make ls use colors
export CLICOLOR=1 
# Use lsd instead to properlty render colors
alias ls="lsd --icon never"
alias lg="lazygit"

export LANG=C.UTF-8
export LC_ALL=C.UTF-8
export UV_MANAGED_PYTHON=true

set -o vi
# Ctrl-P for previous command
bindkey '^P' history-beginning-search-backward
# Ctrl-N for next command 
bindkey '^N' down-line-or-history
# Yazi change directory on exit
function yy() {
	local tmp="$(mktemp -t "yazi-cwd.XXXXXX")"
	yazi "$@" --cwd-file="$tmp"
	if cwd="$(cat -- "$tmp")" && [ -n "$cwd" ] && [ "$cwd" != "$PWD" ]; then
		cd -- "$cwd"
                zle reset-prompt
	fi
	rm -f -- "$tmp"
}
# source ~/powerlevel10k/powerlevel10k.zsh-theme
#
# # To customize prompt, run `p10k configure` or edit ~/.p10k.zsh.
# [[ ! -f ~/.p10k.zsh ]] || source ~/.p10k.zsh
#
# Configure pyenv
# eval "$(pyenv init -)"
# eval "$(pyenv virtualenv-init -)"

# Configure pyenv virtualenv
# eval "$(pyenv virtualenv-init -)"

GPG_TTY=$(tty)
export GPG_TTY
autoload -Uz compinit
# Use cache + skip security scan (safe if you trust your fpath)
compinit -C
export PATH="$HOME/.local/bin:$PATH"
